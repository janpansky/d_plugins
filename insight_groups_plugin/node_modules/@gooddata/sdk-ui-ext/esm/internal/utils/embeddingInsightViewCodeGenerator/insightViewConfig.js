import { DefaultLocale } from "@gooddata/sdk-ui";
import { geoConfigForInsightViewComponent, isGeoChart, } from "../../components/pluggableVisualizations/geoChart/geoConfigCodeGenerator";
/**
 * @internal
 */
export function configForInsightView(insight) {
    if (isGeoChart(insight)) {
        return geoConfigForInsightViewComponent();
    }
}
export function localeForInsightView(ctx) {
    var _a;
    var val = (_a = ctx === null || ctx === void 0 ? void 0 : ctx.settings) === null || _a === void 0 ? void 0 : _a.locale;
    return {
        value: val && val !== DefaultLocale ? val : undefined,
        meta: {
            cardinality: "scalar",
        },
    };
}
//# sourceMappingURL=insightViewConfig.js.map