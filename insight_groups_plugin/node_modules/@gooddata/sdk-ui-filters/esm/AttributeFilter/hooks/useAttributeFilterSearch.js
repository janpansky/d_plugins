// (C) 2022 GoodData Corporation
import { useEffect, useState, useCallback } from "react";
/**
 * Use this hook if you want to implement your custom attribute filter search bar component.
 *
 * @beta
 */
export var useAttributeFilterSearch = function (props) {
    var onSearch = props.onSearch, searchString = props.searchString;
    var _a = useState(searchString), search = _a[0], setSearch = _a[1];
    useEffect(function () {
        setSearch(searchString);
    }, [searchString]);
    var onSearchCallback = useCallback(function (search) {
        setSearch(search);
        onSearch(search);
    }, [onSearch]);
    return {
        onSearch: onSearchCallback,
        search: search,
    };
};
//# sourceMappingURL=useAttributeFilterSearch.js.map