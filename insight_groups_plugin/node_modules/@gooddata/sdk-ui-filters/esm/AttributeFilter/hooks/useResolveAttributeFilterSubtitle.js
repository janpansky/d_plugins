// (C) 2022 GoodData Corporation
import { useIntl } from "react-intl";
import { getElementTitles } from "../utils";
/**
 * @internal
 */
export function useResolveAttributeFilterSubtitle(isCommittedSelectionInverted, committedSelectionElements) {
    var intl = useIntl();
    var isCommittedSelectionEmpty = committedSelectionElements.length === 0;
    var isNone = isCommittedSelectionEmpty && !isCommittedSelectionInverted;
    var isNegativeSelection = !isCommittedSelectionEmpty && isCommittedSelectionInverted;
    var isPositiveSelection = !isCommittedSelectionEmpty && !isCommittedSelectionInverted;
    var subtitle = intl.formatMessage({ id: "gs.list.all" });
    if (isNegativeSelection) {
        subtitle = intl.formatMessage({ id: "gs.list.all" }) + " " + intl.formatMessage({
            id: "gs.list.except",
        }) + " " + getElementTitles(committedSelectionElements, intl);
    }
    else if (isPositiveSelection) {
        subtitle = getElementTitles(committedSelectionElements, intl);
    }
    else if (isNone) {
        subtitle = intl.formatMessage({ id: "gs.filterLabel.none" });
    }
    return subtitle;
}
//# sourceMappingURL=useResolveAttributeFilterSubtitle.js.map