import { MultiSelectAttributeFilterHandler, SingleSelectAttributeFilterHandler } from "./internal";
/**
 * @public
 */
export function newAttributeFilterHandler(backend, workspace, attributeFilter, options) {
    if (options === void 0) { options = { selectionMode: "multi" }; }
    var selectionMode = options.selectionMode, hiddenElements = options.hiddenElements, staticElements = options.staticElements;
    if (selectionMode === "multi") {
        return new MultiSelectAttributeFilterHandler({
            backend: backend,
            workspace: workspace,
            attributeFilter: attributeFilter,
            hiddenElements: hiddenElements,
            staticElements: staticElements,
        });
    }
    return new SingleSelectAttributeFilterHandler({
        backend: backend,
        workspace: workspace,
        attributeFilter: attributeFilter,
        hiddenElements: hiddenElements,
        staticElements: staticElements,
    });
}
//# sourceMappingURL=factory.js.map