// (C) 2021-2022 GoodData Corporation
import { createSlice } from "@reduxjs/toolkit";
import { rootReducers } from "./rootReducers";
import { initialState } from "./state";
var attributeFilterSlice = createSlice({
    name: "attributeFilterSlice",
    // Missing properties are provided in preloadedState in createStore
    initialState: initialState,
    reducers: rootReducers,
});
/**
 * @internal
 */
export var sliceReducer = attributeFilterSlice.reducer;
/**
 * @internal
 */
export var actions = attributeFilterSlice.actions;
//# sourceMappingURL=slice.js.map