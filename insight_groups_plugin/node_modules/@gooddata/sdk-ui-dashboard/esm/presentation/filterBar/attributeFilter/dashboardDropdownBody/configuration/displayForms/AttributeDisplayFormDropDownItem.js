import React from "react";
import cx from "classnames";
import { stringUtils } from "@gooddata/util";
import { ShortenedText } from "@gooddata/sdk-ui-kit";
var getDisplayFormIcon = function (type) {
    switch (type) {
        case "GDC.link":
            return "gd-icon-hyperlink-warning";
        case "GDC.geo.pin":
            return "gd-icon-earth";
        default:
            return "gd-icon-label-warning";
    }
};
var tooltipAlignPoints = [
    { align: "cl cr", offset: { x: -10, y: 0 } },
    { align: "cr cl", offset: { x: 10, y: 0 } },
];
export var AttributeDisplayFormDropDownItem = function (_a) {
    var displayForm = _a.displayForm, onClick = _a.onClick, selected = _a.selected;
    var title = displayForm.title;
    var className = cx("gd-list-item", "attribute-display-form-name", "s-attribute-display-form-name", "s-attribute-display-form-name-" + stringUtils.simplifyText(title), getDisplayFormIcon(displayForm.displayFormType), {
        "is-selected": selected,
    });
    var handleOnClick = function () {
        onClick(displayForm.ref);
    };
    return (React.createElement("div", { className: className, onClick: handleOnClick },
        React.createElement(ShortenedText, { tooltipAlignPoints: tooltipAlignPoints }, title)));
};
//# sourceMappingURL=AttributeDisplayFormDropDownItem.js.map