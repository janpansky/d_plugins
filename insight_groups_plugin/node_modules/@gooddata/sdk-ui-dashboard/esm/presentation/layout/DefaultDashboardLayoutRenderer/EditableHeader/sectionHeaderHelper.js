// (C) 2019-2022 GoodData Corporation
export var MAX_TITLE_LENGTH = 256;
export var MAX_DESCRIPTION_LENGTH = 1024;
export var TITLE_LENGTH_WARNING_LIMIT = 128;
export var DESCRIPTION_LENGTH_WARNING_LIMIT = 512;
export function getTitle(title) {
    if (title && title.length > MAX_TITLE_LENGTH) {
        return title.substring(0, MAX_TITLE_LENGTH) + "...";
    }
    return title;
}
export function getDescription(description) {
    if (description && description.length > MAX_DESCRIPTION_LENGTH) {
        return description.substring(0, MAX_DESCRIPTION_LENGTH) + "...";
    }
    return description;
}
//# sourceMappingURL=sectionHeaderHelper.js.map