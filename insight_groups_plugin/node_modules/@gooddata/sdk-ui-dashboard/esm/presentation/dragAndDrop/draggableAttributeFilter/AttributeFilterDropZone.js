// (C) 2021-2022 GoodData Corporation
import { __read } from "tslib";
import React from "react";
import cx from "classnames";
import { FormattedMessage } from "react-intl";
import { Bubble, BubbleHoverTrigger, Icon } from "@gooddata/sdk-ui-kit";
import { useTheme } from "@gooddata/sdk-ui-theme-provider";
import { useDashboardDrop } from "../useDashboardDrop";
import { useDashboardSelector, selectIsInEditMode } from "../../../model";
import { getDropZoneDebugStyle } from "../debug";
export function AttributeFilterDropZone(_a) {
    var _b, _c;
    var targetIndex = _a.targetIndex, onDrop = _a.onDrop;
    var theme = useTheme();
    var isEditMode = useDashboardSelector(selectIsInEditMode);
    var _d = __read(useDashboardDrop(["attributeFilter", "attributeFilter-placeholder"], {
        drop: function () { return onDrop(targetIndex); },
    }, [targetIndex]), 2), _e = _d[0], canDrop = _e.canDrop, isOver = _e.isOver, itemType = _e.itemType, dropRef = _d[1];
    var isDraggingAttribute = itemType !== undefined && itemType === "attributeFilter";
    if (!isEditMode || isDraggingAttribute) {
        return null;
    }
    var isActive = canDrop || isOver;
    var dropzoneClassNames = cx("attr-filter-dropzone-box", "s-last-filter-drop-position", "s-attr-filter-dropzone-box", { "attr-filter-dropzone-box-active": isActive }, { "attr-filter-dropzone-box-over": isOver });
    var debugStyle = getDropZoneDebugStyle({ isOver: isOver });
    return (React.createElement("div", { className: "attr-filter-dropzone-box-outer", style: debugStyle, ref: dropRef }, isActive ? (React.createElement("div", { className: dropzoneClassNames },
        React.createElement("div", { className: "attr-filter-dropzone-box-inner" },
            React.createElement(FormattedMessage, { id: "filterBar.filter.drop" })))) : (React.createElement(BubbleHoverTrigger, null,
        React.createElement("div", { className: dropzoneClassNames },
            React.createElement("div", { className: "attr-filter-dropzone-box-inner" },
                React.createElement(FormattedMessage, { id: "filterBar.filter.addFilterPlaceholder", values: {
                        icon: (React.createElement(Icon.AttributeFilter, { className: "attribute-filter-icon", width: 14, height: 14, color: (_c = (_b = theme === null || theme === void 0 ? void 0 : theme.palette) === null || _b === void 0 ? void 0 : _b.complementary) === null || _c === void 0 ? void 0 : _c.c6 })),
                    } }))),
        React.createElement(Bubble, { alignPoints: [{ align: "bc tc", offset: { x: 0, y: 0 } }] },
            React.createElement(FormattedMessage, { id: "filterBar.filter.dropzone.tooltip" }))))));
}
//# sourceMappingURL=AttributeFilterDropZone.js.map