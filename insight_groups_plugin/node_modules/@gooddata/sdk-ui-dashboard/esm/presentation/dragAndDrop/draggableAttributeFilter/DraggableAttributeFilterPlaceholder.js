import { __assign, __read } from "tslib";
// (C) 2022 GoodData Corporation
import React from "react";
import { useDashboardDrag } from "../useDashboardDrag";
import classNames from "classnames";
import { useDashboardSelector, selectIsInEditMode } from "../../../model";
export function DraggableAttributeFilterPlaceholder(_a) {
    var PlaceholderComponent = _a.PlaceholderComponent, placeholderComponentProps = _a.placeholderComponentProps;
    var isInEditMode = useDashboardSelector(selectIsInEditMode);
    var _b = __read(useDashboardDrag({
        dragItem: {
            type: "attributeFilter-placeholder",
        },
        canDrag: isInEditMode && !placeholderComponentProps.disabled,
        hideDefaultPreview: false,
    }), 2), isDragging = _b[0].isDragging, dragRef = _b[1];
    return (React.createElement("div", { className: classNames({
            "is-dragging": isDragging,
        }), ref: dragRef },
        React.createElement(PlaceholderComponent, __assign({}, placeholderComponentProps))));
}
//# sourceMappingURL=DraggableAttributeFilterPlaceholder.js.map