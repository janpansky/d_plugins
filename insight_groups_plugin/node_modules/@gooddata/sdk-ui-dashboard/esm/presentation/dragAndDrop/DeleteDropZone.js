import { __read } from "tslib";
// (C) 2022 GoodData Corporation
import cx from "classnames";
import React from "react";
import { FormattedMessage } from "react-intl";
import { removeAttributeFilter, useDashboardDispatch } from "../../model";
import { getDropZoneDebugStyle } from "./debug";
import { useDashboardDrop } from "./useDashboardDrop";
export function DeleteDropZone() {
    var dispatch = useDashboardDispatch();
    var _a = __read(useDashboardDrop("attributeFilter", {
        drop: function (_a) {
            var filter = _a.filter;
            var identifier = filter.attributeFilter.localIdentifier;
            dispatch(removeAttributeFilter(identifier));
        },
    }, [dispatch]), 2), _b = _a[0], canDrop = _b.canDrop, isOver = _b.isOver, dropRef = _a[1];
    if (!canDrop) {
        return null;
    }
    var className = cx("gd-dropzone", "gd-dropzone-delete", {
        "gd-dropzone-over": isOver,
    });
    var debugStyle = getDropZoneDebugStyle({ isOver: isOver });
    return (React.createElement("div", { className: className, ref: dropRef, style: debugStyle },
        React.createElement("div", { className: "gd-dropzone-message" },
            React.createElement(FormattedMessage, { id: "addPanel.deleteItem" }))));
}
//# sourceMappingURL=DeleteDropZone.js.map