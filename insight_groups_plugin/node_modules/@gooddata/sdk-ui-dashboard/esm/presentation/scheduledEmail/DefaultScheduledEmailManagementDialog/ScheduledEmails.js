// (C) 2022 GoodData Corporation
import React from "react";
import { FormattedMessage } from "react-intl";
import { LoadingSpinner } from "@gooddata/sdk-ui-kit";
import { useTheme } from "@gooddata/sdk-ui-theme-provider";
import { ScheduledEmail } from "./ScheduledEmail";
export var ScheduledEmails = function (props) {
    var _a, _b;
    var isLoading = props.isLoading, scheduledEmails = props.scheduledEmails, currentUserEmail = props.currentUserEmail, onDelete = props.onDelete, onEdit = props.onEdit, noSchedulesMessageId = props.noSchedulesMessageId, canManageScheduledMail = props.canManageScheduledMail;
    var theme = useTheme();
    if (isLoading) {
        return (React.createElement("div", { className: "gd-loading-equalizer-wrap" },
            React.createElement("div", { className: "gd-loading-equalizer gd-loading-equalizer-fade" },
                React.createElement(LoadingSpinner, { className: "large gd-loading-equalizer-spinner", color: (_b = (_a = theme === null || theme === void 0 ? void 0 : theme.palette) === null || _a === void 0 ? void 0 : _a.complementary) === null || _b === void 0 ? void 0 : _b.c9 }))));
    }
    if (scheduledEmails.length === 0) {
        return (React.createElement("div", { className: "gd-scheduled-emails-message s-no-schedules-message" },
            React.createElement(FormattedMessage, { id: noSchedulesMessageId, values: { br: React.createElement("br", null) } })));
    }
    return (React.createElement(React.Fragment, null, scheduledEmails.map(function (scheduledEmail) { return (React.createElement(ScheduledEmail, { key: scheduledEmail.identifier, scheduledEmail: scheduledEmail, currentUserEmail: currentUserEmail, onDelete: onDelete, onEdit: onEdit, canManageScheduledMail: canManageScheduledMail })); })));
};
//# sourceMappingURL=ScheduledEmails.js.map