/**
 * Creates and mapping of attribute filter local identifier to connecting attributes matrix indexes.
 */
export function loadFiltersToIndexMapping(filters) {
    var mapping = {};
    var lastIndex = 0;
    filters.forEach(function (filter) { return (mapping[filter.attributeFilter.localIdentifier] = lastIndex++); });
    return mapping;
}
//# sourceMappingURL=loadFiltersToIndexMapping.js.map