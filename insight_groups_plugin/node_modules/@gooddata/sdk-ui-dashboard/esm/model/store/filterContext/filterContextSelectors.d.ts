import { DashboardState } from "../types";
import { ObjRef, FilterContextItem, IDashboardAttributeFilter, IDashboardDateFilter } from "@gooddata/sdk-model";
/**
 * This selector returns original (stored) dashboard's filter context definition.
 *
 * @remarks
 * It is expected that the selector is called only after the filter context state is correctly initialized.
 * Invocations before initialization lead to invariant errors.
 *
 * @returns {@link @gooddata/sdk-backend-spi#IFilterContextDefinition} or `undefined` if original filter context definition is not set.
 *
 * @public
 */
export declare const selectOriginalFilterContextDefinition: import("@reduxjs/toolkit").OutputSelector<DashboardState, import("@gooddata/sdk-model").IFilterContextDefinition | undefined, (res: import("./filterContextState").FilterContextState) => import("@gooddata/sdk-model").IFilterContextDefinition | undefined>;
/**
 * This selector returns original (stored) dashboard's filters.
 *
 * @remarks
 * It is expected that the selector is called only after the filter context state is correctly initialized.
 * Invocations before initialization lead to invariant errors.
 *
 * @returns an array of {@link @gooddata/sdk-backend-spi#FilterContextItem} or an empty array.
 *
 * @public
 */
export declare const selectOriginalFilterContextFilters: import("@reduxjs/toolkit").OutputSelector<DashboardState, FilterContextItem[], (res: import("@gooddata/sdk-model").IFilterContextDefinition | undefined) => FilterContextItem[]>;
/**
 * This selector returns current dashboard's filter context definition.
 *
 * @remarks
 * It is expected that the selector is called only after the filter context state is correctly initialized.
 * Invocations before initialization lead to invariant errors.
 *
 * @returns a {@link @gooddata/sdk-backend-spi#IFilterContextDefinition}
 *
 * @public
 */
export declare const selectFilterContextDefinition: import("@reduxjs/toolkit").OutputSelector<DashboardState, import("@gooddata/sdk-model").IFilterContextDefinition, (res: import("./filterContextState").FilterContextState) => import("@gooddata/sdk-model").IFilterContextDefinition>;
/**
 * Selects dashboard's filter context identity.
 *
 * @remarks
 * The identity may be undefined in two circumstances:
 *
 * -  a new, yet unsaved dashboard; the filter context is saved together with the dashboard and after the
 *    save the identity will be known and added
 *
 * -  export of an existing, saved dashboard; during the export the dashboard receives a temporary
 *    filter context that represents values of filters at the time the export was initiated - which may
 *    be different from what is saved in the filter context itself. that temporary context is not
 *    persistent and lives only for that particular export operation.
 *
 * Invocations before initialization lead to invariant errors.
 *
 * @returns a {@link @gooddata/sdk-backend-spi#IDashboardObjectIdentity} or undefined, if the filter context identity is not set.
 *
 * @internal
 */
export declare const selectFilterContextIdentity: import("@reduxjs/toolkit").OutputSelector<DashboardState, import("@gooddata/sdk-model").IDashboardObjectIdentity | undefined, (res: import("./filterContextState").FilterContextState) => import("@gooddata/sdk-model").IDashboardObjectIdentity | undefined>;
/**
 * Selects list of display form metadata objects referenced by attribute filters.
 *
 * @remarks
 * Invocations before initialization lead to invariant errors.
 *
 * @returns an array of {@link @gooddata/sdk-backend-spi#IAttributeDisplayFormMetadataObject}
 *
 * @public
 */
export declare const selectAttributeFilterDisplayForms: import("@reduxjs/toolkit").OutputSelector<DashboardState, import("@gooddata/sdk-model").IAttributeDisplayFormMetadataObject[], (res: import("./filterContextState").FilterContextState) => import("@gooddata/sdk-model").IAttributeDisplayFormMetadataObject[]>;
/**
 * Selects map of display form metadata objects referenced by attribute filters.
 *
 * @remarks
 * Invocations before initialization lead to invariant errors.
 *
 * @returns a {@link ObjRefMap} of {@link @gooddata/sdk-backend-spi#IAttributeDisplayFormMetadataObject}
 *
 * @internal
 */
export declare const selectAttributeFilterDisplayFormsMap: import("@reduxjs/toolkit").OutputSelector<DashboardState, import("../../../_staging/metadata/objRefMap").ObjRefMap<import("@gooddata/sdk-model").IAttributeDisplayFormMetadataObject>, (res: import("./filterContextState").FilterContextState) => import("../../../_staging/metadata/objRefMap").ObjRefMap<import("@gooddata/sdk-model").IAttributeDisplayFormMetadataObject>>;
/**
 * This selector returns dashboard's filter context filters.
 *
 * @remarks
 * It is expected that the selector is called only after the filter context state is correctly initialized.
 * Invocations before initialization lead to invariant errors.
 *
 * @public
 */
export declare const selectFilterContextFilters: import("@reduxjs/toolkit").OutputSelector<DashboardState, FilterContextItem[], (res: import("@gooddata/sdk-model").IFilterContextDefinition) => FilterContextItem[]>;
/**
 * This selector returns dashboard's filter context attribute filters.
 *
 * @remarks
 * It is expected that the selector is called only after the filter context state is correctly initialized.
 * Invocations before initialization lead to invariant errors.
 *
 * @public
 */
export declare const selectFilterContextAttributeFilters: import("@reduxjs/toolkit").OutputSelector<DashboardState, IDashboardAttributeFilter[], (res: FilterContextItem[]) => IDashboardAttributeFilter[]>;
/**
 * This selector returns dashboard's filter context date filter.
 *
 * @remarks
 * It is expected that the selector is called only after the filter context state is correctly initialized.
 * Invocations before initialization lead to invariant errors.
 *
 * @public
 */
export declare const selectFilterContextDateFilter: import("@reduxjs/toolkit").OutputSelector<DashboardState, IDashboardDateFilter | undefined, (res: FilterContextItem[]) => IDashboardDateFilter | undefined>;
/**
 * Creates a selector for selecting attribute filter by its displayForm {@link @gooddata/sdk-model#ObjRef}.
 *
 * @remarks
 * Invocations before initialization lead to invariant errors.
 *
 * @public
 */
export declare const selectFilterContextAttributeFilterByDisplayForm: (displayForm: ObjRef) => import("@reduxjs/toolkit").OutputSelector<DashboardState, IDashboardAttributeFilter | undefined, (res1: import("../../../_staging/metadata/objRefMap").ObjRefMap<import("@gooddata/sdk-model").IAttributeDisplayFormMetadataObject>, res2: IDashboardAttributeFilter[]) => IDashboardAttributeFilter | undefined>;
/**
 * Creates a selector for selecting attribute filter by its localId.
 *
 * @remarks
 * Invocations before initialization lead to invariant errors.
 *
 * @public
 */
export declare const selectFilterContextAttributeFilterByLocalId: (localId: string) => import("@reduxjs/toolkit").OutputSelector<DashboardState, IDashboardAttributeFilter | undefined, (res: IDashboardAttributeFilter[]) => IDashboardAttributeFilter | undefined>;
/**
 * Creates a selector for selecting attribute filter index by its localId.
 *
 * @remarks
 * Invocations before initialization lead to invariant errors.
 *
 * @public
 */
export declare const selectFilterContextAttributeFilterIndexByLocalId: (localId: string) => import("@reduxjs/toolkit").OutputSelector<DashboardState, number, (res: IDashboardAttributeFilter[]) => number>;
/**
 * Creates a selector for selecting all descendants of the attribute filter with given localId.
 *
 * @remarks
 * Invocations before initialization lead to invariant errors.
 *
 * @public
 */
export declare const selectAttributeFilterDescendants: (localId: string) => import("@reduxjs/toolkit").OutputSelector<DashboardState, string[], (res: IDashboardAttributeFilter[]) => string[]>;
/**
 * Creates a selector for selecting all filters with different reference than the given one.
 *
 * @internal
 */
export declare const selectOtherContextAttributeFilters: (ref?: import("@gooddata/sdk-model").UriRef | import("@gooddata/sdk-model").IdentifierRef | undefined) => import("@reduxjs/toolkit").OutputSelector<DashboardState, IDashboardAttributeFilter[], (res: IDashboardAttributeFilter[]) => IDashboardAttributeFilter[]>;
/**
 * Creates a selector for selecting filter identifier to index map.
 *
 * @remarks
 * The mapping's purpose is to enable efficient handling and reading from the connecting attributes
 * matrix.
 *
 * @see {@link selectConnectingAttributesMatrix} for details.
 *
 * @internal
 */
export declare const selectFiltersToIndexMap: import("@reduxjs/toolkit").OutputSelector<DashboardState, Record<string, number> | undefined, (res: import("./filterContextState").FilterContextState) => Record<string, number> | undefined>;
/**
 * Creates a selector for selecting connecting attributes matrix.
 *
 * @remarks
 * The matrix is used to check if the pair of attribute filters are able to create parent child
 * relationship.
 *
 * @internal
 */
export declare const selectConnectingAttributesMatrix: import("@reduxjs/toolkit").OutputSelector<DashboardState, import("../..").ConnectingAttributeMatrix | undefined, (res: import("./filterContextState").FilterContextState) => import("../..").ConnectingAttributeMatrix | undefined>;
/**
 * Creates a selector to get a display form of the filter defined by its local identifier.
 *
 * @internal
 */
export declare const selectAttributeFilterDisplayFormByLocalId: (localId: string) => import("@reduxjs/toolkit").OutputSelector<DashboardState, ObjRef, (res: IDashboardAttributeFilter[]) => ObjRef>;
/**
 * Creates a selector which checks for a circular dependency between filters.
 *
 * @internal
 */
export declare const selectIsCircularDependency: (currentFilterLocalId: string, neighborFilterLocalid: string) => import("@reduxjs/toolkit").OutputSelector<DashboardState, boolean, (res: string[]) => boolean>;
/**
 * Creates a selector returning connecting attributes for a two attribute filters defined by their local identifiers.
 *
 * @internal
 */
export declare const selectConnectingAttributesForFilters: (currentFilterLocalId: string, neighborFilterId: any) => import("@reduxjs/toolkit").OutputSelector<DashboardState, import("../..").IConnectingAttribute[], (res1: Record<string, number> | undefined, res2: import("../..").ConnectingAttributeMatrix | undefined) => import("../..").IConnectingAttribute[]>;
//# sourceMappingURL=filterContextSelectors.d.ts.map