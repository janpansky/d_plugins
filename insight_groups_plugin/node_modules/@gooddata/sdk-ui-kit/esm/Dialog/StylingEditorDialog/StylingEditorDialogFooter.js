// (C) 2022 GoodData Corporation
import React from "react";
import noop from "lodash/noop";
import { Button } from "../../Button";
import { useIntl } from "react-intl";
import { LoadingSpinner } from "../../LoadingSpinner";
import { Bubble, BubbleHoverTrigger } from "../../Bubble";
import { Hyperlink } from "../../Hyperlink";
import { Footer } from "../Footer";
import { FooterButtons } from "../FooterButtons";
/**
 * @internal
 */
export var StylingEditorDialogFooter = function (props) {
    var link = props.link, _a = props.disableSubmit, disableSubmit = _a === void 0 ? false : _a, _b = props.showProgressIndicator, showProgressIndicator = _b === void 0 ? false : _b, errorMessage = props.errorMessage, onSubmit = props.onSubmit, onCancel = props.onCancel, _c = props.onHelpClick, onHelpClick = _c === void 0 ? noop : _c;
    var intl = useIntl();
    return (React.createElement(Footer, null,
        React.createElement(Hyperlink, { text: link.text, href: link.url, onClick: function () { return onHelpClick(); }, iconClass: "gd-icon-circle-question" }),
        React.createElement(FooterButtons, null,
            showProgressIndicator ? (React.createElement(LoadingSpinner, { className: "gd-loading-equalizer-spinner small s-gd-styling-editor-spinner" })) : null,
            React.createElement(Button, { className: "gd-button-secondary s-dialog-cancel-button", value: intl.formatMessage({ id: "cancel" }), onClick: function () { return onCancel(); } }),
            React.createElement(BubbleHoverTrigger, { className: "gd-button", showDelay: 0, hideDelay: 0 },
                React.createElement(Button, { className: "gd-button-action s-dialog-submit-button", value: intl.formatMessage({ id: "save" }), onClick: function () { return onSubmit(); }, disabled: disableSubmit }),
                errorMessage && disableSubmit ? (React.createElement(Bubble, { className: "bubble-negative", alignPoints: [{ align: "tc br" }] }, errorMessage)) : null))));
};
//# sourceMappingURL=StylingEditorDialogFooter.js.map