// (C) 2022 GoodData Corporation
import React from "react";
import cx from "classnames";
import { DialogListLoading } from "./DialogListLoading";
import { DialogListEmpty } from "./DialogListEmpty";
import { DialogListItemBasic } from "./DialogListItemBasic";
/**
 * @internal
 */
export var DialogList = function (props) {
    var items = props.items, isLoading = props.isLoading, className = props.className, emptyMessageElement = props.emptyMessageElement, itemComponent = props.itemComponent, itemClassName = props.itemClassName, onItemClick = props.onItemClick, onItemDelete = props.onItemDelete;
    if (isLoading) {
        return React.createElement(DialogListLoading, null);
    }
    if (items.length === 0) {
        return React.createElement(DialogListEmpty, { message: emptyMessageElement });
    }
    var ListItemComponent = itemComponent !== null && itemComponent !== void 0 ? itemComponent : DialogListItemBasic;
    var classNames = cx("gd-dialog-list-wrapper s-dialog-list-wrapper", className);
    return (React.createElement("div", { className: classNames },
        React.createElement("div", { className: "gd-dialog-list" }, items.map(function (item) { return (React.createElement(ListItemComponent, { key: item.id, className: itemClassName, item: item, onClick: onItemClick, onDelete: onItemDelete })); }))));
};
//# sourceMappingURL=DialogList.js.map