import { __assign } from "tslib";
// (C) 2022 GoodData Corporation
import React from "react";
import { FormattedMessage, useIntl } from "react-intl";
import { HeightSetting } from "./HeightSetting";
import { ToggleSwitch } from "./ToggleSwitch";
/**
 * @internal
 */
export var OptionsByReference = function (props) {
    var option = props.option, onChange = props.onChange;
    var intl = useIntl();
    return (React.createElement("div", { className: "embed-insight-dialog-lang-selector" },
        React.createElement("strong", { className: "bottom-space top-space" },
            React.createElement(FormattedMessage, { id: "embedInsightDialog.code.options" })),
        React.createElement(ToggleSwitch, { id: "display-title", className: "bottom-space top-space", label: intl.formatMessage({
                id: "embedInsightDialog.code.options.display.title",
            }), checked: option.displayTitle, onChange: function () {
                var opt = __assign(__assign({}, option), { displayTitle: !option.displayTitle });
                onChange(opt);
            } }),
        React.createElement(ToggleSwitch, { id: "custom-height", label: intl.formatMessage({
                id: "embedInsightDialog.code.options.custom.height",
            }), checked: option.customHeight, onChange: function () {
                var opt = __assign(__assign({}, option), { customHeight: !option.customHeight });
                onChange(opt);
            } }),
        option.customHeight ? (React.createElement(HeightSetting, { value: option.height, unit: option.unit, onValueChange: function (height, unit) {
                var opt = __assign(__assign({}, option), { height: height, unit: unit });
                onChange(opt);
            } })) : null));
};
//# sourceMappingURL=OptionsByReference.js.map