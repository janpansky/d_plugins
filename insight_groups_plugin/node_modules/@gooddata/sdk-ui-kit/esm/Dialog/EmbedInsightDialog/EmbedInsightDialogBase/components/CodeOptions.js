// (C) 2022 GoodData Corporation
import React from "react";
import { OptionsByDefinition } from "./OptionsByDefinition";
import { OptionsByReference } from "./OptionsByReference";
/**
 * @internal
 */
export var CodeOptions = function (props) {
    var option = props.option, onChange = props.onChange;
    if (option.type === "definition") {
        return React.createElement(OptionsByDefinition, { option: option, onChange: onChange });
    }
    return React.createElement(OptionsByReference, { option: option, onChange: onChange });
};
//# sourceMappingURL=CodeOptions.js.map