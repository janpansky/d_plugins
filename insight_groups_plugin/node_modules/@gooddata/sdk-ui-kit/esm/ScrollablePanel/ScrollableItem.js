// (C) 2022 GoodData Corporation
import React, { useEffect, useRef } from "react";
import { useScrollContext } from "./ScrollContext";
/**
 * @internal
 */
export var ScrollableItem = function (props) {
    var item = useRef(null);
    var scroll = useScrollContext();
    var scrollIntoView = props.scrollIntoView, bottomMargin = props.bottomMargin, isElementInvisibleCheck = props.isElementInvisibleCheck, className = props.className, children = props.children, _a = props.tagName, TagName = _a === void 0 ? "div" : _a;
    useEffect(function () {
        if (scrollIntoView) {
            var element = item.current;
            scroll.scrollIntoView(element, bottomMargin, isElementInvisibleCheck);
        }
    }, [bottomMargin, isElementInvisibleCheck, scroll, scrollIntoView, item]);
    return (React.createElement(TagName, { className: className, ref: item }, children));
};
//# sourceMappingURL=ScrollableItem.js.map