// (C) 2022 GoodData Corporation
import throttle from "lodash/throttle";
import CustomEventPolyfill from "custom-event"; // TODO: FET-772 Remove IE-specific polyfills, upgrade blocked dependencies
var fireGoodstrapDragEvent = function (node, windowInstance) {
    if (windowInstance === void 0) { windowInstance = { dispatchEvent: function (_event) { return true; } }; }
    windowInstance.dispatchEvent(new CustomEventPolyfill("goodstrap.drag", {
        // this will close dropdowns with closeOnMouseDrag=true
        bubbles: true,
        detail: {
            node: node,
        },
    }));
};
/**
 * This is custom dom goodstrap event, it is used by Overlay to handle CloseOnParentDrag
 * This event is throttled by default
 * @internal
 */
export var handleOnGoodstrapDragEvent = throttle(function () { return fireGoodstrapDragEvent(document.body, window); }, 500);
//# sourceMappingURL=drag.js.map